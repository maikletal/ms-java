# App (base settings for app)
spring.banner.location=log/banner.txt
spring.application.name=@project.artifactId@
spring.project.version=@version@

# Profiles (set one or multiple profiles for load settings)
spring.profiles.active=${spring_profile:local}

# App (base settings for app)
server.port=${server_port:8090}

# Logging
logging.level.org.springframework=${log_level_springframework:warn}
logging.level.com.prosegur=${log_level_app:debug}
logging.level.root=${log_level_root:warn}
logging.config=classpath:log/logback-spring.xml

# Config parameters
msjava.param.example=${msjava_param_example:controlador_test}
